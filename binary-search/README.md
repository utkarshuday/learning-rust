# Binary Search

Implemented binary search algorithm which works on all slices which contains elements which can be ordered. Additionally this can work not only on slices, but at the same time also on a Vec or an Array.
